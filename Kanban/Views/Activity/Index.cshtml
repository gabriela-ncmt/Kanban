@model List<ActivityModel>

    <div class="container">
        <div class="row">
            <div class="col-4">
                <h1>Pending</h1>
            @if(Model.Count() > 0)
            {
                @foreach (var activities in Model.Where(x => x.Status.Name == "Pending"))
                {
                    <div class="card text-center">
                        <div class="card-header bg-primary">
                            @activities.Status.Name
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">@activities.Title</h5>
                            <p class="card-text">@activities.Description</p>
                            <a href="#">Next Card</a>
                        </div>
                        <div class="card-footer text-body-secondary">
                            @activities.CreatedDate.ToString("dd/MM/yyyy")
                        </div>
                    </div>
                }
            }
        </div>

            <div class="col-4">
            <h1>OnGoing</h1>
            @if (Model.Count() > 0)
            {
                @foreach (var activities in Model.Where(x => x.Status.Name == "OnGoing"))
                {
                    <div class="card text-center">
                        <div class="card-header bg-warning">
                            @activities.Status.Name
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">@activities.Title</h5>
                            <p class="card-text">@activities.Description</p>
                            <a href="#">Next Card</a>
                        </div>
                        <div class="card-footer text-body-secondary">
                            @activities.CreatedDate.ToString("dd/MM/yyyy")
                        </div>
                    </div>
                }
            }

            </div>
            
            <div class="col-4">
            <h1>Done</h1>
            @if (Model.Count() > 0)
            {
                @foreach (var activities in Model.Where(x => x.Status.Name == "Done"))
                {
                    <div class="card text-center">
                        <div class="card-header bg-success">
                            @activities.Status.Name
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">@activities.Title</h5>
                            <p class="card-text">@activities.Description</p>
                            <a href="#">Delete Card</a>
                        </div>
                        <div class="card-footer text-body-secondary">
                            @activities.CreatedDate.ToString("dd/MM/yyyy")
                        </div>
                    </div>
                }
            }
            </div>
        </div>
    </div>